language: node_js

branches:

    only:
        - master
        - development

node_js:
  - "7"

before_install:
  - npm i -g npm@5.8.0
  - npm install -g truffle
  - npm install -g ganache-cli

cache:
  directories:
    - node_modules

install:
  - truffle install

before_script:
  - nohup ganache-cli &
  - sleep 1

script:
  - truffle test
